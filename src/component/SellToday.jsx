import DashList from "../structure/page/dashboard/DashList";

const SellToday = () => {
  return (
    <div className="w-full mx-auto">
      <DashList value={true} />
    </div>
  );
};

export default SellToday;
